[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
dependencies = ["typer"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[project.scripts]
todo = "app.views.cli:app"