[project]
name = "todo-cli"
version = "0.1.0"
description = "CLI de gerenciamento de tarefas"
dependencies = ["typer"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[project.scripts]
todo = "app.views.cli:app"